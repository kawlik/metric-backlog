export default function (props: { action(): void }) {
	// component logic

	// component layout
	return (
		<input
			className="bg-gray-200 px-4 py-3 rounded-md"
			placeholder="🔍 Search"
			type="text"
		/>
	);
}
